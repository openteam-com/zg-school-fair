<%= render :partial => 'about_fair' if params[:event_id] == "school-fair-about" %>
<div class="row reviews-wrapper">
  <% @related_items.each do |key, value| %>
    <%= render partial: "commons/posters/review", locals: { info: value  } %>
  <% end %>
</div>
